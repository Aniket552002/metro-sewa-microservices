services:
  mariadb-ticket:
    image: mariadb:11
    container_name: mariadb-ticket
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: metro_ticket_db
    ports:
      - "3308:3306"
    volumes:
      - ticket_data:/var/lib/mysql

volumes:
  ticket_data: